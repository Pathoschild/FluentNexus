namespace Pathoschild.FluentNexus.Models
{
    /// <summary>The type of an entry in an archive file.</summary>
    public enum ContentPreviewEntryType
    {
        /// <summary>The entry is a file.</summary>
        File,

        /// <summary>The entry is a folder.</summary>
        Directory
    }
}
